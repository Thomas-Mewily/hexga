use super::*;

pub type VecF<const N: usize> = Vector<float, N>;
pub type VecU<const N: usize> = Vector<uint, N>;
pub type VecI<const N: usize> = Vector<int, N>;
pub type VecB<const N: usize> = Vector<bool, N>;
